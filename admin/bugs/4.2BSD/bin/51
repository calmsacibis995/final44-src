Return-Path: <jxs7451%rit.csnet@csnet-relay.arpa>
Date: Thu, 30 Jan 86 16:21:27 est
From: D <jxs7451%rit.csnet@csnet-relay.arpa>
Subject: 4.2BSD UNIX grep command
Index: bin/grep 4.2BSD

Description:
	'grep' when redirected to a file that is the last file 
	alphabetically, searches the file that it is redirected to
	causing an infite loop.
Repeat-By:
	type the following:
		grep mov * > zzz
	This example is done in a directory that contains assembally
	source code.  Also zzz is assumed to be the last file in
	the directory.
