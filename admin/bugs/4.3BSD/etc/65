From mcvax!cwi.nl!root@seismo.css.gov  Fri May 29 09:36:44 1987
Date: Fri, 29 May 87 12:32:08 +0200
From: mcvax!cwi.nl!root@seismo.css.gov (Ed Gronke)
Subject: problem with config, wrong names for swap*.c in CFILES
Index: etc/config/mkmakefile.c 4.3BSD beta for tahoe (CCI)

Description:
	The swapnames source names in CFILES in makefiles
	generated by config is wrong.

Repeat-By:
	run config. cd to the /sys/MACHINE directory. "make depend".
	Notice that it complains about "../swap*.c: no source file"

Fix:
	------- mkmakefile.c -------
	440c440
	< 			fprintf(fp, "../%s ", swapname);
	---
	> 			fprintf(fp, "./%s ", swapname);

-- Ed Gronke (epg@mcvax.cwi.nl)
