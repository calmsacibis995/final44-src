*** Makefile	Tue Mar 24 09:42:03 1987
--- Makefile.new	Tue Mar 24 09:42:24 1987
***************
*** 11,17 ****
  # Programs that live in subdirectories, and have makefiles of their own.
  #
  SUBDIR=	config dump dump.4.1 fsck ftpd getty htable implog named restore \
! 	routed talkd termcap tftpd timed XNSrouted
  
  # Shell scripts that need only be installed and are never removed.
  #
--- 11,17 ----
  # Programs that live in subdirectories, and have makefiles of their own.
  #
  SUBDIR=	config dump dump.4.1 fsck ftpd getty htable implog named restore \
! 	routed talkd termcap tftpd timed tzone XNSrouted
  
  # Shell scripts that need only be installed and are never removed.
  #
