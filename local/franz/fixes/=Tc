This directory contains fixes to Lisp/Liszt.  They are numbered
Xn.m  where X = l means to Lisp, = z means to Liszt
 n is the version number of Lisp or Liszt and m is the fix number, beginning
 with 1
Each fix is described here.

Lisp fixes:

l34.1 - make alphalessp return nil for equal values, and allow it to work
	with strings.

l34.2 - fix documentation on implode

l34.3 - fix fix function

Liszt fixes:

z5.1 - fix maps over multiple lists to stop whenever any of the lists
	is exausted.

z5.2 - add specials function to handle (declare (specials t))
