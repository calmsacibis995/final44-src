>> replace all (Push  stack atm) with (push atm stack)

>> Attempt to remove all machine differences from the files, place all
   differences in the new files mda.l, mdb.l , etc (machine dependent 'a', 'b',
   etc)

