This tape was written at 1600 BPI using the following command:

	find . -print | cpio -ocB > /dev/rmt1

Unload the tape with the following list of commands:

	mkdir forms.d
	cd forms.d
	cpio -icdBu < /dev/rmt1

Read the README file for directions from there.
