cd ${GUIDELIB:-/usr/lib/guide}
test -d man || mkdir man
cd man
test -d cachel || mkdir cachel
test -d cache1 || mkdir cache1
test -d cache2 || mkdir cache2
test -d cache3 || mkdir cache3
test -d cache4 || mkdir cache4
test -d cache5 || mkdir cache5
test -d cache6 || mkdir cache6
test -d cache7 || mkdir cache7
test -d cache8 || mkdir cache8
test -d cachel || mkdir cachel
chmod 777 cache*
